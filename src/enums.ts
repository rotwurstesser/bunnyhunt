export enum TerrainType {
    Water,
    Land
}

export enum GroundType {
    Dirt,
    ForestFloor
}

export enum PlantType {
    None,
    Grass,
    AridGrass,
    Oak,
    Pine
}

export enum AnimalType {
    Rabbit, // Herbivore
    Wolf    // Carnivore
}